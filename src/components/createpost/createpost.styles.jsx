import styled from 'styled-components';

import {Button} from 'react-bootstrap';

export const RapperCreatePostComponent = styled.div`
  color: black;

`;
export const BUTTON = styled(Button)`

  margin: 2px;
`;